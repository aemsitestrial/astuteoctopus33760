# Feature Cards

Displays a header (label, title, subtitle) followed by a row of feature cards.

## Note for authors

**This block shows a maximum of 2 feature cards.** You can add more cards in the
editor, but only the first two will appear on the published page — any additional
cards are ignored during rendering. If you need to feature more than two items,
remove the extras or use a different block.
