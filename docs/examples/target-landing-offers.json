{
  "_comment": "Sample Adobe Target Form-Based (JSON) offers for the landing page. Create one JSON offer per scope in Target (Offers > Create > JSON Offer) and assign it to the experience for that decision scope. Each top-level key is the decision scope / mbox name (matches a handler in scripts/target.js). Field names inside `set` match that block's fieldMap. `_note` keys are documentation only — omit them from the real offer.",

  "hero": {
    "_note": "Block-level fields. Omit any field you don't want to change.",
    "set": {
      "badge": "Rated #1 for customer trust — 2026",
      "heading": "Energy that works as hard as you do — Experience A",
      "subtitle": "Fair, transparent pricing and 100% renewable supply for homes and businesses across the UK.",
      "primaryCta": "Get your quote",
      "secondaryCta": "Compare plans"
    }
  },

  "metrics": {
    "_note": "Item collection. `match.item` selects a metric by its current label text; `set` updates that item's value/label/change. This example targets all three metrics in the block.",
    "items": [
      {
        "match": { "item": "Current rate / kWh" },
        "set": { "value": "£0.22", "label": "Current rate / kWh", "change": "↓ 8.3% vs last month" }
      },
      {
        "match": { "item": "Renewable mix today" },
        "set": { "value": "91%", "label": "Renewable mix today", "change": "↑ 19% vs avg" }
      },
      {
        "match": { "item": "Active customers" },
        "set": { "value": "2.4M", "label": "Active customers", "change": "+38k this month" }
      }
    ]
  },

  "_metrics_multi_instance_example": {
    "_note": "Use this shape (still under the `metrics` scope) when the page has MORE THAN ONE metrics block. `match.key` picks the block by its data-target-key (authored in UE); `match.item` then picks the metric within that block. Rename this key to `metrics` to use it, and give each metrics block a distinct data-target-key.",
    "items": [
      {
        "match": { "key": "hero-kpis", "item": "Current rate / kWh" },
        "set": { "value": "£0.22", "change": "↓ 8.3% vs last month" }
      },
      {
        "match": { "key": "hero-kpis", "item": "Renewable mix today" },
        "set": { "value": "91%", "change": "↑ 19% vs avg" }
      },
      {
        "match": { "key": "footer-kpis", "item": "Active customers" },
        "set": { "value": "2.4M", "change": "+38k this month" }
      },
      {
        "match": { "key": "footer-kpis", "item": "CO₂ saved" },
        "set": { "value": "1.2M tonnes", "change": "↑ 6% YoY" }
      }
    ]
  },

  "stats": {
    "_note": "Item collection matched by label. Update value and/or label.",
    "items": [
      {
        "match": { "item": "Avg. annual saving vs big six" },
        "set": { "value": "£415", "label": "Avg. annual saving vs big six" }
      },
      {
        "match": { "item": "Trustpilot rating" },
        "set": { "value": "4.9★", "label": "Trustpilot rating" }
      }
    ]
  },

  "feature-cards": {
    "_note": "Block-level header fields for the feature-cards section.",
    "set": {
      "label": "Why switch to Xcel",
      "heading": "Green energy, made simple and honest — Experience A",
      "subtitle": "No exit fees, no bill shock, no asterisks. Just fair energy powered by wind, solar, and hydro."
    }
  },

  "usage-dashboard": {
    "_note": "Block-level fields. `cta` is only applied if this dashboard instance has a CTA link.",
    "set": {
      "label": "Smart metering",
      "heading": "Your meter. Your data. Your control. — Experience A"
    }
  },

  "page": {
    "_note": "Composite offer — one scope drives several blocks at once. `blocks` maps each block/scope name to that block's normal offer shape. Use this instead of separate hero/metrics/feature-cards offers when a single experience spans multiple blocks.",
    "blocks": {
      "hero": {
        "set": {
          "heading": "Welcome back — Experience A",
          "subtitle": "Fair, transparent, renewable energy — picked up where you left off."
        }
      },
      "metrics": {
        "items": [
          { "match": { "item": "Current rate / kWh" }, "set": { "value": "£0.22", "change": "↓ 8.3% vs last month" } },
          { "match": { "item": "Renewable mix today" }, "set": { "value": "91%", "change": "↑ 19% vs avg" } },
          { "match": { "item": "Active customers" }, "set": { "value": "2.4M", "change": "+38k this month" } }
        ]
      },
      "feature-cards": {
        "set": { "heading": "Why switch to Xcel — Experience A" }
      }
    }
  }
}
